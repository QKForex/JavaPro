package com.qkforex.dev.concurrency.chapter02;

public class ThreadConstructor {

    public static void main(String[] args) {
        /*Thread t1=new Thread("T1");
        ThreadGroup group=new ThreadGroup("testgroup");
        Thread t2=new Thread(group,"T2");
        ThreadGroup mainThreadGroup=Thread.currentThread().getThreadGroup();
        System.out.println("main thread belong to "+mainThreadGroup.getName());
        System.out.println("t1 thread and main belong to mainThreadGroup " +(t1.getThreadGroup()==mainThreadGroup));
        System.out.println("t2 thread group not belong to main group  " +(mainThreadGroup==t2.getThreadGroup()));
        System.out.println("t2 thread belong to main testgroup  " +(group==t2.getThreadGroup()));*/




    }
}
